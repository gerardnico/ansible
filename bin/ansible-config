#!/bin/bash

SCRIPT_PATH="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

args=("$@")

if [ "${1:-}" == "synopsis" ]; then
  args=("-h")
fi

"${SCRIPT_PATH}/ans-x-docker-run.sh" ansible-config "${args[@]}"
