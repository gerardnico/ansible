export DOCK_X_REGISTRY=ghcr.io
export DOCK_X_NAME=ansible
export DOCK_X_NAMESPACE=gerardnico
# !!! Tag change
# if you change the tag don't forget to change it in the
# code (ie ans-x-env.sh)
# Otherwise you may not understand why you get the bad version
export DOCK_X_TAG=9.12.0
export BUILD_CONTEXT=Dockerfiles/$DOCK_X_TAG
export PATH=$PWD/contrib:$PWD/bin:$PATH
